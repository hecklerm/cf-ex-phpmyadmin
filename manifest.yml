---
applications:
- name: phpmyadmin
  memory: 128M 
  instances: 1
  host: phpmyadmin-${random-word}
  path: .
  buildpack: https://github.com/cloudfoundry/php-buildpack.git
  services:
  - quotesdb
